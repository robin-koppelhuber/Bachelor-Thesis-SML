# @package _global_
method:
  name: averaging
  class_path: src.methods.averaging.AveragingMerging
  description: "Weighted averaging of task vectors (data-free baseline)"

  params:
    normalize_preferences: true
    separate_heads: true  # Merge encoder only; route each task to its fine-tuned classifier head

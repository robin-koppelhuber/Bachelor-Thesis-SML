# @package _global_
benchmark:
  name: recovery_cola
  description: "Single-task recovery ablation: CoLA only. Verifies Chebyshev/EPO can match textattack fine-tuned quality."

  tasks:
    - cola

  preference_vectors:
    - [1.0]

  evaluation:
    batch_size: 32
    num_samples: null
    reference_num_samples: null
    # Single-task benchmark: cross_eval with 1 expert is degenerate (nadir = utopia).
    # Use random_baseline for single-task recovery runs.
    nadir_method: "random_baseline"
    metrics:
      - name: accuracy
        plots: []
      - name: f1_macro
        plots: []
      - name: cohen_kappa
        plots: []
      - name: mcc
        plots: []
    cross_metric_plots: []

  mode: train_eval
  cache_enabled: true
  force_retrain: false
  clear_cache: false
